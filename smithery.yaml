# Smithery configuration for AI Scout MCP Server
# https://smithery.ai/docs/build/project-config/smithery-yaml

runtime: "container"

startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
  exampleConfig: {}

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
